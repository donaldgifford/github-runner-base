# Summary

<!-- What changed and why. Link the related issue: Fixes #123 / Refs #123 -->

## Release impact

<!--
Releases are driven by this PR's semver label on merge to main:
  major / minor / patch — bump the version, build and publish the image
  dont-release          — no version bump, nothing publishes
Pick exactly one. CI blocks the PR until you do.
-->

- [ ] Labeled `major`, `minor`, `patch`, or `dont-release`

## Test plan

<!--
How you verified the change. `just ci` runs the same lint, build, tool
verification, and vulnerability gate that CI does.
-->

- [ ] `just ci` passes locally
